# youtube-clone-html-css
A pixel-accurate clone of the YouTube homepage built using semantic HTML and modern CSS. Includes a fully responsive layout (desktop, tablet, mobile), a pure-CSS dark/light theme switch using :has(), and an off-canvas mobile sidebar — built to practice real-world front-end layout and theming techniques.
